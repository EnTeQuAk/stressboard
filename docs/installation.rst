============
Installation
============

At the command line::

    $ easy_install stressboard

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv stressboard
    $ pip install stressboard